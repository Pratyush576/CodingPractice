package org.pk.practices.dsa;

public class TrappingRainWaterProblem {
    int maxWater(int[] arr) {
        int length = arr.length;
        if (length < 3) {
            return 0;
        }

        int maxLeft = arr[0];
        int maxRight = arr[arr.length - 1];
        // Considering maxLeft as smallest of
        for (int i = 0; i < arr.length; i++) {

        }
        return 0;
    }
}
