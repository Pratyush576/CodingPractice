package org.pk.practices.servicesmarketplace.pro;

/** DESIGN.md §1.7/§11 Phase 5 — a real transition is added later; Phase 1 just carries the column. */
public enum VerificationStatus {
    UNVERIFIED,
    VERIFIED
}
