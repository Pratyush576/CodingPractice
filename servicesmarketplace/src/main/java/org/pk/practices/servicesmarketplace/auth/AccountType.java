package org.pk.practices.servicesmarketplace.auth;

/** Which table an authenticated identity resolved against — Customer and Pro are distinct entities. */
public enum AccountType {
    CUSTOMER,
    PRO
}
